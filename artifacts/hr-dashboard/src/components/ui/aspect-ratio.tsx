/**
 * @fileoverview Custom UI Aspect Ratio wrapper component.
 * Restricts height proportionally relative to width.
 */

import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
